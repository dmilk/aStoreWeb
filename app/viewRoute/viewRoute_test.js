'use strict';

describe('aStore.viewRoute module', function() {

  beforeEach(module('aStore.viewRoute'));

  describe('viewRoute controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ViewRouteCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});