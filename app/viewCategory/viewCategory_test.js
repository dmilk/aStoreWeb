'use strict';

describe('aStore.viewCategory module', function() {

  beforeEach(module('aStore.viewCategory'));

  describe('viewCategory controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCategoryCtrl = $controller('ViewCategoryCtrl');
      expect(viewCategoryCtrl).toBeDefined();
    }));

  });
});