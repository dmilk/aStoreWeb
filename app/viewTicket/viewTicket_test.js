'use strict';

describe('aStore.viewTicket module', function() {

  beforeEach(module('aStore.viewTicket'));

  describe('viewTicket controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewTicketCtrl = $controller('ViewTicketCtrl');
      expect(viewTicketCtrl).toBeDefined();
    }));

  });
});