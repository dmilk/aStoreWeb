'use strict';

describe('aStore.viewLogin module', function() {

  beforeEach(module('aStore.viewLogin'));

  describe('viewLogin controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ViewLoginCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});