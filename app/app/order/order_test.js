'use strict';

describe('aStore.viewOrder module', function() {

  beforeEach(module('aStore.viewOrder'));

  describe('viewOrder controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewOrderCtrl = $controller('ViewOrderCtrl');
      expect(viewOrderCtrl).toBeDefined();
    }));

  });
});