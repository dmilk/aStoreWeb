'use strict';

describe('aStore.viewCheckout module', function() {

  beforeEach(module('aStore.viewCheckout'));

  describe('viewCheckout controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ViewCheckoutCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});