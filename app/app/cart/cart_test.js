'use strict';

describe('aStore.viewCart module', function() {

  beforeEach(module('aStore.viewCart'));

  describe('viewCart controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ViewCartCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});